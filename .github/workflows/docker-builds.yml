name: docker-builds

on:
  workflow_dispatch:
  pull_request:
    paths:
      - .ci/docker/**
      - .github/workflows/docker-builds.yml
  push:
    branches:
      - main
      - release/*
    paths:
      - .ci/docker/**
      - .github/workflows/docker-builds.yml
  schedule:
    - cron: 1 3 * * 3

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

permissions: {}

jobs:
  docker-build:
    permissions:
      contents: read
      id-token: write
    runs-on: [self-hosted, linux.2xlarge]
    timeout-minutes: 240
    strategy:
      fail-fast: false
      matrix:
        include:
          - docker-image-name: torchtitan-ubuntu-20.04-clang12
    env:
      DOCKER_IMAGE: 308535385114.dkr.ecr.us-east-1.amazonaws.com/torchtitan/${{ matrix.docker-image-name }}
    steps:
      - name: Clean workspace
        shell: bash
        run: |
          echo "${GITHUB_WORKSPACE}"
          sudo rm -rf "${GITHUB_WORKSPACE}"
          mkdir "${GITHUB_WORKSPACE}"

      - name: Setup SSH (Click me for login details)
        uses: pytorch/test-infra/.github/actions/setup-ssh@8211ac1658f19599a28ef46aae7fbac932e93189  # main
        with:
          github-secret: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout the repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Setup Linux
        uses: pytorch/test-infra/.github/actions/setup-linux@8211ac1658f19599a28ef46aae7fbac932e93189  # main

      - name: Build docker image
        id: build-docker-image
        uses: pytorch/test-infra/.github/actions/calculate-docker-image@8211ac1658f19599a28ef46aae7fbac932e93189  # main
        with:
          docker-image-name: ${{ matrix.docker-image-name }}
          always-rebuild: true
          push: true
          force-push: true

      - name: Teardown Linux
        uses: pytorch/test-infra/.github/actions/teardown-linux@8211ac1658f19599a28ef46aae7fbac932e93189  # main
        if: always()
